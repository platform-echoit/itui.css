// What the README tells consumers to do.
import { createRoot } from 'react-dom/client';
import { Button } from '@echoit/itui.css';

createRoot(document.getElementById('root')!).render(<Button>one</Button>);
